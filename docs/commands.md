# Command Reference

dbai has 6 commands, each registered as a Cobra command on the root command.

## Global Behavior

- All commands support `--help` (auto-generated by Cobra)
- All output goes to stdout; informational messages go to stderr
- Exit code 0 = success, non-zero = error (specific codes depend on the error)
- AI features require `OPENAI_API_KEY` environment variable
- Config is loaded from `~/.dbai/config.json` on every command invocation

---

## `dbai connect <dsn>`

Connect to a database and introspect its schema.

```
dbai connect postgres://user:pass@localhost:5432/mydb
dbai connect mysql:user:pass@tcp(localhost:3306)/mydb
dbai connect sqlite:/path/to/database.db
dbai connect sqlite:///path/to/database.db
```

### DSN Formats

| Database   | DSN Prefix       | Example                                                    |
|------------|------------------|------------------------------------------------------------|
| PostgreSQL | `postgres://`    | `postgres://user:pass@localhost:5432/mydb?sslmode=disable` |
| PostgreSQL | `postgresql://`  | `postgresql://user@localhost/mydb` (alias, same driver)    |
| MySQL      | `mysql://`       | `mysql://user:pass@tcp(localhost:3306)/mydb`               |
| SQLite     | `sqlite:`        | `sqlite:/data/app.db`                                      |
| SQLite     | `sqlite://`      | `sqlite:///data/app.db`                                    |

### What Connect Does

1. Detects the database driver from the DSN prefix
2. Opens a connection (TCP for PG/MySQL, file for SQLite)
3. Pings the database to verify connectivity
4. Introspects the schema (tables, columns, types, PKs, nullability, defaults, indexes)
5. Caches the schema to `~/.dbai/schema.json`
6. Persists the DSN for automatic reconnection
7. Sets this connection as "active" for subsequent commands

### Introspection Details

**PostgreSQL:**
- Queries `information_schema.tables` + `information_schema.columns` + `information_schema.key_column_usage` for PK detection
- Queries `pg_index` + `pg_class` + `pg_attribute` for index discovery
- Schema: `public` only (user tables only, no system schemas)

**MySQL:**
- Queries `information_schema.TABLES` + `information_schema.COLUMNS` with `COLUMN_KEY = 'PRI'` for PK detection
- Queries `information_schema.STATISTICS` for index discovery
- Schema: `DATABASE()` (current database only)

**SQLite:**
- Queries `sqlite_master` for user tables (excludes `sqlite_%` internal tables)
- Uses `PRAGMA table_info(name)` for column metadata
- Uses `PRAGMA index_list(name)` + `PRAGMA index_info(name)` for index discovery
- Enables WAL mode via `PRAGMA journal_mode=WAL`

### Output

```
✓ Connected to PostgreSQL (***@localhost:5432/mydb)
  Schema loaded: 12 tables, 89 columns, 15 indexes
```

---

## `dbai query [question|sql]`

Execute a SQL query or translate a natural language question into SQL and run it.

```
dbai query "SELECT * FROM users LIMIT 5"
dbai query "show me users who ordered last month"
dbai query "top 10 products by revenue" --dry-run
dbai query "total sales last quarter" --explain
dbai query "recent orders" --json
dbai query "recent orders" --csv
```

### Argument Detection

The `looksLikeSQL()` function determines whether input is raw SQL or natural language:

1. Strips leading whitespace from the input
2. Checks the first 10 non-whitespace characters against SQL keywords:
   - `SELECT`, `WITH`, `EXPLAIN`, `INSERT`, `UPDATE`, `DELETE`
   - `CREATE`, `ALTER`, `DROP`, `TRUNCATE`
3. If it matches any keyword → treated as SQL (executed directly)
4. Otherwise → treated as natural language (sent to AI)

### Natural Language → SQL Flow

1. Load cached schema from `~/.dbai/schema.json`
2. If no schema cached or OPENAI_API_KEY not set, returns a placeholder SQL:
   ```sql
   -- Set OPENAI_API_KEY for AI-powered queries
   -- Schema loaded. Run: export OPENAI_API_KEY="sk-..."
   SELECT 1;
   ```
3. Calls OpenAI Chat Completions API with:
   - **System prompt**: Schema description + SQL generation rules
   - **User prompt**: The natural language question
   - **Model**: From config (`openai_model`), env var (`OPENAI_MODEL`), or `gpt-4`
   - **Temperature**: 0.1 (deterministic, low creativity)
4. Cleans the response (strips markdown code fences)
5. Validates the generated SQL via `EXPLAIN` (syntax check)
6. If validation fails → returns error with the invalid SQL for debugging

### Flags

| Flag         | Description                                                     | Default |
|--------------|-----------------------------------------------------------------|---------|
| `--json`     | Output results as a JSON array of objects                       | false   |
| `--csv`      | Output results as CSV (comma-separated, quoted if needed)       | false   |
| `--dry-run`  | Show generated SQL without executing                            | false   |
| `--explain`  | Show execution plan alongside results                           | false   |

### Output Format Precedence

1. `--json` flag → JSON format
2. `--csv` flag → CSV format
3. Config `output_format` field (if set to `"json"` or `"csv"`)
4. Default → table format

### Dry-Run Mode

When `--dry-run` is specified:
- NL input generates SQL via AI (consumes tokens)
- SQL input is displayed as-is
- No database query is executed
- The SQL is printed to stdout

### Explain Mode

When `--explain` is specified:
- Runs `EXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)` for PostgreSQL
- Runs `EXPLAIN ANALYZE` for MySQL
- Runs `EXPLAIN QUERY PLAN` for SQLite
- Shows the plan on stderr, then executes the query and shows results on stdout
- If EXPLAIN fails (e.g., DDL statement), the error is printed but query still runs

### AI Validation

For AI-generated SQL (not raw SQL input), the generated query is validated with `EXPLAIN` before execution. If validation fails:

```
Error: generated SQL is invalid — try rephrasing your question:
  SELECT * FORM users
  ERROR: syntax error at or near "FORM"
```

---

## `dbai analyze [query]`

Run EXPLAIN ANALYZE on a query and surface performance issues. This is dbai's most powerful feature for PostgreSQL databases.

```
dbai analyze "SELECT * FROM orders WHERE created_at > now() - interval '30 days'"
dbai analyze --all
```

### How It Works

1. Attempts JSON-format EXPLAIN first: `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)`
2. If JSON succeeds → parses the plan tree into a structured Report
3. If JSON fails (MySQL, SQLite don't support JSON format) → falls back to text EXPLAIN
4. Analysis includes sub-second timing via `time.Since(start)`

### JSON Plan Parsing

The `analyze` package parses PostgreSQL's JSON EXPLAIN output. The plan tree is:

```
RawPlan              Top-level: Plan root + Planning Time + Execution Time
  └── RawNode        A single plan node (Seq Scan, Hash Join, Index Scan, etc.)
       ├── NodeType          "Seq Scan", "Hash Join", "Index Scan", "Nested Loop"
       ├── RelationName      Table name (e.g., "users", "orders")
       ├── ActualTotal       Actual execution time in ms
       ├── ActualRows        Actual number of rows processed
       ├── PlanRows          Estimated rows by the planner
       ├── Filter            WHERE clause pushed to the scan node
       ├── IndexName         Index name (for Index Scan / Index Only Scan)
       ├── StartupCost       Estimated startup cost
       ├── TotalCost         Estimated total cost
       ├── JoinType          "Hash", "Nested Loop", "Merge"
       ├── HashCond          Hash join condition
       └── Plans[]           Child nodes (recursive)
```

The parser flattens this tree into a list of `FlatNode` structs with depth information (for indented tree display).

### Detected Issues (5 Categories)

| # | Issue | Severity | Threshold | Suggestion |
|---|-------|----------|-----------|------------|
| 1 | Sequential scan on large table | 🟡 Warning | >100 actual rows | Add an index on WHERE/JOIN columns |
| 2 | Row estimate mismatch | 🟡 Warning | >10x or <0.1x ratio | Run ANALYZE or adjust statistics target |
| 3 | Expensive filter on seq scan | ℹ️ Info | >1ms filter time | Index the filtered column |
| 4 | Nested Loop with many rows | ℹ️ Info | >1000 rows | Index on inner table join column |
| 5 | Slow individual node | 🔴 Critical | >100ms | Query rewrite or index strategy |

### Report Format

```
Execution Time: 12.50 ms
Planning Time: 0.15 ms

Scan Summary:
  Sequential Scans: 2
  Index Scans: 0
  ⚠ Row Estimate Mismatches: yes (worst: 4.0x)
  Heaviest Node: Seq Scan on orders (3.2ms)

Plan Tree:
  Hash Join on  (12.3ms, 950 rows)
    Seq Scan on users (0.5ms, 150 rows)
    Seq Scan on orders (3.2ms, 8000 rows)

Issues:
🟡 Sequential scan on orders (8000 rows, 3.2ms)
   → Consider adding an index on orders for columns used in WHERE or JOIN conditions
ℹ Filter applied on sequential scan: (created_at > now() - interval '30 days') (3.2ms)
   → Consider an index on the filtered column(s)
```

### `--all` Flag

Analyzes all cached tables by generating `SELECT * FROM <table> LIMIT 1000` for each table and running EXPLAIN ANALYZE. Only shows tables with detected issues (clean tables are noted on stderr but not printed to stdout).

```
Analyzing all tables in mydb...

users:
  ✅ No issues found

orders (12.50ms):
  🟡 Sequential scan on orders (8000 rows, 3.2ms)
```

### Database Support

| Database   | JSON Plans | Text Plans | Issue Detection |
|------------|:----------:|:----------:|:---------------:|
| PostgreSQL | ✅ Yes     | ✅ Fallback | ✅ Full analysis |
| MySQL      | ❌ No      | ✅ Text     | ⚠️ Partial (text display only) |
| SQLite     | ❌ No      | ✅ QUERY PLAN | ⚠️ Partial (no timing/rows) |

---

## `dbai repl`

Interactive database shell with AI-powered query assistance.

```
dbai repl
dbai repl --format json
```

### Dot Commands

| Command | Description |
|---------|-------------|
| `.help` | Show available commands |
| `.quit` or `.exit` | Exit the REPL |
| `.tables` | List all tables with column counts |
| `.schema` | Show full schema (columns, types, PKs, nullability, indexes) |
| `.connect <dsn>` | Connect to a different database mid-session |

### How It Works

1. On start: loads config, tries active connection or `default_dsn` from config
2. Displays a prompt: `dbai> `
3. Reads input line-by-line via `bufio.Scanner`
4. Empty lines are skipped
5. Dot-commands are handled directly
6. Everything else goes through the same NL→SQL pipeline as `dbai query`

### REPL Query Flow

Same as `dbai query`:
1. `looksLikeSQL()` check → NL or SQL
2. NL → load schema → AI call → generate SQL → execute
3. SQL → execute directly
4. Output formatted according to `--format` flag or config default

### Auto-Connect

The REPL tries to connect automatically on startup:
1. Active connection (from a previous `dbai connect`)
2. `default_dsn` from config file
3. Legacy DSN cache
4. If nothing works → starts disconnected with guidance message

### Format Flag

```
dbai repl --format json
dbai repl --format csv
```

Sets the output format for the entire REPL session. Can also be set via config file.

---

## `dbai completion <shell>`

Generate shell completion scripts for bash, zsh, fish, or powershell.

```
source <(dbai completion bash)         # Bash (current session)
source <(dbai completion zsh)          # Zsh (current session)
dbai completion fish | source          # Fish (current session)
dbai completion powershell | Out-String | Invoke-Expression  # PowerShell
```

### Persisting Completions

```bash
# Bash — add to ~/.bashrc
source <(dbai completion bash)

# Or system-wide
dbai completion bash > /etc/bash_completion.d/dbai
```

### Supported Shells

| Shell       | Generator Function                       |
|-------------|------------------------------------------|
| bash        | `rootCmd.GenBashCompletion()`            |
| zsh         | `rootCmd.GenZshCompletion()`             |
| fish        | `rootCmd.GenFishCompletion(_, true)`     |
| powershell  | `rootCmd.GenPowerShellCompletionWithDesc()` |

### Error Handling

Unsupported shells return: `unsupported shell: <name> (use bash, zsh, fish, or powershell)`

---

## `dbai version`

Print version and build information.

```
$ dbai version
dbai dev
  Go version: go1.25.0
  Platform: linux/amd64
  Commit: 4692dc5
```

### Build Info Fields

| Field      | Source                                     | Example              |
|------------|--------------------------------------------|----------------------|
| Version    | `debug.ReadBuildInfo().Main.Version`       | `v1.0.0`, `dev`     |
| Go version | `runtime.Version()`                        | `go1.25.0`          |
| Platform   | `runtime.GOOS` / `runtime.GOARCH`          | `linux/amd64`       |
| Commit     | `vcs.revision` setting (first 7 chars)     | `4692dc5`           |
| Build time | `vcs.time` setting (first 10 chars)        | `2026-05-18`        |
| Modified   | `vcs.modified` setting                     | `yes` (if dirty)    |

The version command uses Go's built-in VCS info embedded at build time (since Go 1.18, enabled by default). No `-ldflags` injection needed.
